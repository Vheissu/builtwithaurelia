{
  "rules": {
    ".read": true,
    "submissions": {
      "$slug": {
        ".write": "auth.uid != null",
        ".indexOn": "added",
        "votes": {
          "$uid": {
            ".write": "auth.uid === $uid"
          }
        }
      }
    }
  }
}
