{
  "rules": {
    ".read": true,
    "submissions": {
      ".indexOn": ["added", "_uid"],
      "$slug": {
        ".write": "auth.uid != null",
        "votes": {
          "$uid": {
            ".write": "auth.uid === $uid"
          }
        }
      }
    }
  }
}
